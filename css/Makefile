# Compile LESS files into CSS

.PHONY: default prod-build dev-build css clean
default: css

prod-build: css
dev-build: css

css:
	$(error 'This has been deprecated. Run `make` in parent dir to build all assets');

clean:
	rm -f *.css
